{
  "name": "prime-check",
  "version": "0.0.0",
  "description": "Check if a number is a prime number or not",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jake Rowsell-Messchaert",
  "license": "ISC",
  "dependencies": {
    "chokidar-cli": "^1.2.0",
    "jest": "^19.0.2"
  }
}
